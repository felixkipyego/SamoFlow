# evals

Golden sets and the eval runner for task 3.7 (see docs/SPEC.md sections 13 and
18). This is a placeholder today: no golden sets or scoring logic exist yet.

## Running

```
python evals/run.py
```

from the repo root.

The runner exits 0 when there is nothing to evaluate, so CI stays green until
real evals are added.
