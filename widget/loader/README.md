# widget/loader

The widget's loader script (`widget.js`): reads `data-site-key`, renders the
launcher inside a small shadow root, and loads the chat UI on first click
(docs/SPEC.md §11).

Owned by step 5.1 (Loader and shadow DOM shell) — see PROJECT_SPEC.md.

Placeholder: no build tooling yet. The bundler is chosen in step 5.1.
