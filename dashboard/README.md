# dashboard

The React dashboard SPA: tenant settings, sources and ingestion status,
escalation inbox, usage against plan limits, and platform admin
(docs/SPEC.md §12).

Owned by steps 6.1 to 6.5 (Dashboard authentication, Tenant settings,
Sources screens, Escalation inbox and usage, Platform admin) — see
PROJECT_SPEC.md.

Placeholder: no build tooling yet. The dashboard tooling is chosen in step 6.1.
