# backend/app/mcp/service.py
# Step 1.1b: whole-application skeleton stub for mcp's future service.
def search_knowledge_base():
    # TODO(3.6): the MCP knowledge-search tool wrapper the agent calls (the
    # model has no tools with side effects). Inputs: the tenant id and
    # query. Outputs: the tool's search results.
    raise NotImplementedError
