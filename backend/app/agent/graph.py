# backend/app/agent/graph.py
# Step 1.1b: whole-application skeleton stub for agent's future graph.
def build_graph():
    # TODO(3.3): build the graph skeleton and rule-based router that
    # dispatches to the Retrieval, Fallback & Guardrail and Escalation
    # Coordinator specialists. Inputs: none (constructs the graph). Outputs:
    # a compiled graph. Answer generation, citations and follow-up rewrite
    # are added from Step 3.4 onward.
    raise NotImplementedError
