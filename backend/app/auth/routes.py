# backend/app/auth/routes.py
# Step 1.1b: whole-application skeleton stub for auth's future endpoint.
def create_session():
    # TODO(1.4): issue and verify the widget's session (JWT-based). Inputs:
    # the incoming request (site key, origin). Outputs: a session token /
    # verified visitor identity.
    raise NotImplementedError
