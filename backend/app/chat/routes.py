# backend/app/chat/routes.py
# Step 1.1b: whole-application skeleton stub for chat's future endpoint.
def stream_chat_response():
    # TODO(4.1): the chat endpoint (SSE) that streams the agent's answer to
    # the widget. Inputs: the chat request (message, conversation id).
    # Outputs: a server-sent-events stream of the answer.
    raise NotImplementedError
