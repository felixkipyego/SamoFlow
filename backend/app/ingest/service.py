# backend/app/ingest/service.py
# Step 1.1b: whole-application skeleton stub for ingest's future service.
def enqueue_ingestion_job():
    # TODO(2.1): ingestion tables and job queue for a tenant's content
    # sources (URL list, crawl, uploads, database sync). Inputs: a tenant's
    # content source. Outputs: a queued ingestion job. Adapters, safe
    # fetching and scheduling are added from Step 2.2 onward.
    raise NotImplementedError
