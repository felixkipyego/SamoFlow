# backend/app/tenancy/repository.py
# Step 1.1b: whole-application skeleton stub for tenancy's future repository.
def get_tenant_scoped_repository():
    # TODO(1.2): return the tenant-scoped repository helper that all
    # tenant-scoped database access must go through (never raw client_id
    # filtering at the call site). Inputs: the verified tenant identity.
    # Outputs: a repository object scoped to that tenant.
    raise NotImplementedError
