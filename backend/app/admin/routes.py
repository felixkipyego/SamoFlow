# backend/app/admin/routes.py
# Step 1.1b: whole-application skeleton stub for admin's future endpoint.
def list_platform_tenants():
    # TODO(6.5): platform-admin endpoints (e.g. listing/suspending tenants,
    # revoking domain verification). Inputs: the platform admin's verified
    # identity. Outputs: tenant records. UNCERTAIN: this package's owning
    # step spans 6.1-6.5 (dashboard/platform-admin backend, per its own
    # __init__.py NOTE); 6.5 Platform admin was picked as the closest single
    # match since the package is named "admin" specifically, not "dashboard".
    raise NotImplementedError
