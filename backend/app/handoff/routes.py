# backend/app/handoff/routes.py
# Step 1.1b: whole-application skeleton stub for handoff's future endpoint.
def submit_escalation():
    # TODO(4.4): the escalation endpoint the visitor's browser submits to
    # for human handoff. Inputs: the escalation request (contact details,
    # conversation context). Outputs: a created escalation record.
    raise NotImplementedError
