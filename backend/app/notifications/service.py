# backend/app/notifications/service.py
# Step 1.1b: whole-application skeleton stub for notifications' future service.
def send_email():
    # TODO(4.3): send email (quota/usage alerts, escalation notifications)
    # through our own SMTP server behind a provider-neutral interface.
    # Inputs: the recipient, subject and body. Outputs: none (side effect:
    # email sent).
    raise NotImplementedError
